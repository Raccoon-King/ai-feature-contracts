# Grabby Task Brief: Adaptive Agent Routing by Stage and Substep

## Request
Refine and improve agent routing to choose best agent per step and substep

## Ticket
- Who: Grabby maintainers and operators
- What: Refine agent routing to select the best specialist by stage and substep
- Why: Current keyword-heavy routing can misroute complex work; we need deterministic, state-aware handoffs
- Definition of Done:
  - state-first routing used whenever artifact state is known
  - governance gates enforced before execution/audit
  - substep-aware routing matrix implemented
  - confidence scoring and fallback implemented
  - routing memory persisted and used at selection time
  - checkpoint re-evaluation implemented
  - misrouting regression tests added and passing
  - decision trace exposed in CLI/debug output

## Facilitator
- Persona: Archie
- Role: Contract Architect
- Mode: contract
- Why this persona: Use a full contract interview when scope or shape is still emerging.

## Objective
Implement state-aware, substep-aware, confidence-aware routing so Grabby selects the best agent at each step/substep while preserving governance gates

## Scope Breakdown
- Replace keyword-first routing with state-first routing
- Enforce artifact governance gates in routing
- Add substep-aware routing model
- Add confidence scoring and fallback routing
- Add routing memory and outcome feedback
- Add re-evaluation checkpoints after major transitions
- Expand tests for known misrouting scenarios
- Add routing observability and explainability

## Constraints
Preserve existing contract lifecycle commands and backward compatibility of public CLI commands

## Done When
state-first routing used whenever artifact state is known, governance gates enforced before execution/audit, substep-aware routing matrix implemented, confidence scoring and fallback implemented, routing memory persisted and used at selection time, checkpoint re-evaluation implemented, misrouting regression tests added and passing, decision trace exposed in CLI/debug output

## Recommended Handoff
`grabby agent architect CC`
