# FC: Consolidate test fixtures and helpers
**ID:** DX-003 | **Status:** draft
**Type:** refactor

## Ticket
- Who: Grabby test maintainers
- What: Extract repeated test fixtures into shared utilities
- Why: 2,400+ line test files with duplicated createLogger, writeValidContract patterns

## Objective
Create `tests/fixtures/` with shared test utilities to reduce test file sizes by 30% and improve test maintainability.

## Scope
- Extract `createLogger()` mock factory
- Extract `writeValidContract(dir, id, content)` helper
- Extract `createTestContext(overrides)` helper
- Extract sample contract fixtures (valid, invalid, edge cases)
- Update existing tests to use shared fixtures

## Non-Goals
- No new test coverage (just refactoring)
- No changes to test runner config

## Directories
**Allowed:** `tests/`
**Restricted:** `lib/`, `node_modules/`

## Files
| Action | Path | Reason |
|--------|------|--------|
| create | `tests/fixtures/index.js` | Shared test utilities |
| create | `tests/fixtures/contracts.js` | Sample contract content |
| create | `tests/fixtures/mocks.js` | Logger, fs, and other mocks |
| modify | `tests/commands.test.js` | Use shared fixtures |
| modify | `tests/features.test.js` | Use shared fixtures |
| modify | `tests/interactive-workflows.test.js` | Use shared fixtures |
| modify | `tests/interactive-shell.test.js` | Use shared fixtures |

## Dependencies
- No new dependencies

## Done When
- [ ] `tests/fixtures/` created with index.js, contracts.js, mocks.js
- [ ] createLogger() extracted and used in 10+ test files
- [ ] writeValidContract() extracted and used in 5+ test files
- [ ] commands.test.js reduced by 200+ lines
- [ ] All tests still pass

## Testing
- Run: `npm test` - all existing tests pass
- Verify: No test behavior changes
